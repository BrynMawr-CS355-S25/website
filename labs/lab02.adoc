include::header.adoc[]

:title: Processes II
:Author Initials: ASN
:Date:    2020-08-15
:Revision: 0.1
:ASST: labs-USERNAME

= {title}

**Lab exercises for January 30th**

The goals for this assignment are:

* Working with C strings
* Working with processes

:sectnums:

#include <stdio.h>
#include <string.h>
#define delims " \t"

void print(char** list)
{
    printf("[");
    for (char** ptr = list; *ptr != NULL; ptr++)
    {
        printf("%s, ", *ptr);
    }
    printf("NULL]\n");
}

int main()
{
    char line[1024];
    strcpy(line, "ls -l -a");    
    
    int n = 0;
    char* list[128]; // max of 128 words can be put in the list
    char* token = strtok(line, delims);
    while (token)
    {
        list[n++] = token; 
        token = strtok(NULL, delims);
    }
    list[n] = NULL;
    print(list);
}

= Zombie


= Orphan


= Signal Handler


= Alarm



